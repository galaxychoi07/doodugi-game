# 부동산 두더지 게임

정적 HTML 1파일 앱입니다.

## 실행
압축을 풀고 `index.html`을 브라우저에서 열면 바로 실행됩니다.

## 배포
Vercel, Netlify, GitHub Pages, Blogger HTML 위젯 iframe 등으로 배포 가능합니다.

## 블로그 삽입 추천
네이버 블로그는 스크립트 실행 제한이 있어 직접 삽입보다 외부 배포 URL 링크 버튼 방식이 안전합니다.
블로그스팟은 HTML 보기에서 iframe 삽입이 가능합니다.
